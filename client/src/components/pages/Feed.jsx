import React, {useEffect, useState} from "react";
// TODO (Step 1.1): import SingleStory

const Feed = () => {
  

  useEffect(() => {
  }, []);

  return <div>This is the feed!</div>;
  // TODO (Step 1.1): render a SingleStory with hardcoded props
};

export default Feed;
